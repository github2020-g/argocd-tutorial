apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: git-directories-generator-ex1
  namespace: argocd
spec:
  generators:
  - git:
      repoURL: https://github.com/github2020-g/argocd-tutorial.git
      revision: main
      directories:
      - path: V09-applicationSet-Person/helmChart/*
  template:
    metadata:
      name: '{{path.basename}}-application'
    spec:
      project: default
      source:
        repoURL: https://github.com/github2020-g/argocd-tutorial.git
        targetRevision: main
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{path.basename}}-ns'
      syncPolicy:
        automated: {}
        syncOptions:
          - CreateNamespace=true
